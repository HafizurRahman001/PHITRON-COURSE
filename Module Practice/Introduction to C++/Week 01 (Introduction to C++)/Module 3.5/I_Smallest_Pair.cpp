#include <bits/stdc++.h>
using namespace std;

int main()
{

    int n;
    cin >> n;

    int x;
    int a[x];
    for (int i = 0; i < n; i++)
    {
        cin >> x;
        for (int i = 0; i < x; i++)
        {
            cin >> a[i];
        }
    }

    // printing the array
    for (int i = 0; i < 5; i++)
    {
        cout << "h" << endl;
    }

    return 0;
}