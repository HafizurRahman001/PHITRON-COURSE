// ceil()    // #include <math.h>
// floor()    // #include <math.h>
// round()    // #include <math.h>
// sqrt()    // #include <math.h>
// pow()    // #include <math.h>
// abs()    // #include <math.h>

#include <stdio.h>
#include <math.h>

int main()
{
    // ceil function
    // double x;
    // scanf("%lf", &x);

    // int ans = ceil(x);
    // printf("%d", ans);

    // floor function
    // double x;
    // scanf("%lf", &x);

    // int ans = floor(x);
    // printf("%d", ans);

    // round function
    // double x;
    // scanf("%lf", &x);

    // int ans = round(x); // যদি দশমিকের পর 5 বা তার বেশি পাই তাহলে এটা তার ceil করে ফেলে (৩.৫ --> ৪, ২.৯ --> ৩), আর যদি দশমিকের পরের অংক ৫ এর কম থাকে তাহলে floor করে (2.3 --> 2, 3.4 -->3)
    // printf("%d", ans);

    // sqrt() function
    // double x;
    // scanf("%lf", &x);

    // double ans = sqrt(x);
    // printf("%0.2lf", ans);

    // pow () function
    // int x, y;
    // scanf("%d %d", &x, &y);

    // int ans = pow(x, y);
    // printf("%d", ans);

    // abs function
    int x;
    scanf("%d", &x);

    int ans = abs(x);
    printf("%d", ans);

    return 0;
}
